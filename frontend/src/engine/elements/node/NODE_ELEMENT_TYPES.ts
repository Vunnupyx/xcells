/**
 * these a used to identify the node elements within the events
 */
enum NODE_ELEMENT_TYPES {
  container = 'container',
  mask = 'mask',
  background = 'background',
  image = 'image',
  children = 'children',
  text = 'text',
  html = 'html',
  graphics = 'graphics',
  headerHandle = 'headerHandle',
  downloadHandle = 'downloadHandle',
  resizeHandle = 'resizeHandle',
  childGhostContainer = 'childGhostContainer',
  headerMarkups = 'headerMarkups',
  tag = 'tag',
  checkBox = 'checkBox',
}

export default NODE_ELEMENT_TYPES
